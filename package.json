{
  "name": "mailer",
  "version": "1.0.0",
  "description": "Node ka mailer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"npm install --prefix portfolio && npm run build --prefix portfolio --output public",
    "install-client":"cd portfolio && npm install",
    "start": "node index.js",
    "client":"cd portfolio && npm start",
    "heroku-postbuild": "npm install --prefix portfolio && npm run build --prefix portfolio"
  },
  "engines": {
  "node": "16.x"
},
  "author": "Abhay Singh",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^6.3.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-handlebars": "^5.3.4",
    "googleapis": "^89.0.0",
    "heroku": "^7.59.1",
    "nodemailer": "^6.7.0",
    "nodemon": "^2.0.14"
  }
}
